tools:
  git:
    version: latest
    win:
      choco: "git"
    mac:
      brew: "git"
    linux:
      apt: "git"

  vscode:
    version: latest
    win:
      choco: "vscode"
    mac:
      brew_cask: "visual-studio-code"
    linux:
      apt: "code"

  docker:
    version: latest
    win:
      choco: "docker-desktop"
    mac:
      brew_cask: "docker"
    linux:
      apt: "docker.io"
      post:
        - "scripts/linux/post/docker-group.sh"

  nerd_fonts:
    version: latest
    win:
      script: "scripts/windows/install-nerd-fonts.ps1"
    mac:
      script: "scripts/macos/install-nerd-fonts.sh"
    linux:
      script: "scripts/linux/install-nerd-fonts.sh"

  cursor:
    version: latest
    win:
      choco: "cursoride"
    mac:
      brew_cask: "cursor"
    linux:
      script: "scripts/linux/install-cursor.sh"

  oh_my_posh:
    version: latest
    win:
      choco: "oh-my-posh"
    mac:
      script: "scripts/macos/install-oh-my-posh.sh"
    linux:
      script: "scripts/linux/install-oh-my-posh.sh"

  bitwarden:
    version: latest
    win:
      choco: "bitwarden"
    mac:
      brew_cask: "bitwarden"
    linux:
      apt: "bitwarden"

  google_chrome:
    version: latest
    win:
      choco: "googlechrome"
    mac:
      brew_cask: "google-chrome"
    linux:
      script: "scripts/linux/install-chrome.sh"

  zoom:
    version: latest
    win:
      choco: "zoom"
    mac:
      brew_cask: "zoom"
    linux:
      script: "scripts/linux/install-zoom.sh"

  uv:
    version: latest
    win:
      script: "scripts/windows/install-uv.ps1"
    mac:
      script: "scripts/macos/install-uv.sh"
    linux:
      script: "scripts/linux/install-uv.sh"

  fnm:
    version: latest
    win:
      choco: "fnm"
    mac:
      script: "scripts/macos/install-fnm.sh"
    linux:
      script: "scripts/linux/install-fnm.sh"

  chrome_extensions:
    version: latest
    win:
      script: "scripts/windows/install-chrome-extensions.ps1"
    mac:
      script: "scripts/macos/install-chrome-extensions.sh"
    linux:
      script: "scripts/linux/install-chrome-extensions.sh"
